apiVersion: v1
kind: Service
metadata:
  name: {{ include "goadmin.fullname" . }}
  labels:
    {{- include "goadmin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "goadmin.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
