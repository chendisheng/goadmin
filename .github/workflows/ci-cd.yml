# Deprecated backup workflow.
# The active CI workflow is .github/workflows/ci.yml.
# The active release workflow is .github/workflows/release.yml.

name: CI/CD (Deprecated Backup)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: echo "Use .github/workflows/ci.yml and .github/workflows/release.yml instead."
