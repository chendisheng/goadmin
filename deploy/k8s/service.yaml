apiVersion: v1
kind: Service
metadata:
  name: goadmin
  namespace: goadmin
  labels:
    app.kubernetes.io/name: goadmin
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: goadmin
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 80
      targetPort: http
