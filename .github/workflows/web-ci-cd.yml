# Deprecated backup workflow.
# The active frontend build/publish logic is part of .github/workflows/release.yml.

name: Frontend CI/CD (Deprecated Backup)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: echo "Use .github/workflows/ci.yml and .github/workflows/release.yml instead."
