apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goadmin
spec:
  rules:
    - host: goadmin.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            server:
              service:
                name: goadmin
                port:
                  name: http
