apiVersion: v1
kind: Service
metadata:
  name: goadmin-web
  namespace: goadmin
  labels:
    app.kubernetes.io/name: goadmin
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: goadmin
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
